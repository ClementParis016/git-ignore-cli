'use strict';

const expect = require('chai').expect;
const gitIgnore = require('./lib/index.js');

describe('.gitignore path lookup', () => {

  it('should retrieve path to local .gitignore', () => {
    // todo
  });

  it('should retrieve path to global .gitignore', () => {
    // todo
  });

});

describe('.gitignore data insertion', () => {

  it('should append .gitignore data', () => {
    // todo
  });

});

describe('.gitignore data retrieval', () => {

  it('should return .gitignore data', () => {
    // todo
  });

});
